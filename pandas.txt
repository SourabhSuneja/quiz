In Pandas, a data table is called a ________ (series, DataFrame, table, array)
All missing values in a DataFrame are filled with _________ by default.
How will you create a series containing runs scored by 3 different batsmen indexed by their names?
Which attribute is used to set the name of a series?
In Pandas, axis=1 represents _________ (row/column).To display the first 3 elements of a series S, we can write __________ (A) S[:3], B) S[3], C) S[1:3], D) All of these)
What type of error is seen when the number of indices passed doesn't match number of elements in a series? (Value error, Syntax error, Name error, Logical error)
